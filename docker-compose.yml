version: '3.7'

services:
  app:
    build:
      context: .
    volumes:
      - .:/var/www/html
    restart: unless-stopped
    environment:
      UID: ${UID}
      GID: ${GID}
    tty: true
    stdin_open: true